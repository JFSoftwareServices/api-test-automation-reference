# Allure results directory — relative to project root.
# The Allure TestNG listener reads this on startup to know where to write
# the JSON result files that are later assembled into the HTML report.
# Must be on the test classpath (src/test/resources/).
allure.results.directory=target/allure-results