# Base configuration file
# Default values used when no runtime overrides are provided.

# Override priority (highest → lowest):
# 1. JVM system properties (-Dkey=value)
# 2. Environment variables (KEY=value)
# 3. This file

log.requests=false
log.responses=false
suite=testng-parallel.xml