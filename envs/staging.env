# envs/staging.env
#
# Docker environment variables for the staging test container.
#
# Used by:
#   docker compose --profile staging up tests-staging
#
# These variables are injected into the container at runtime and may be used
# by the application to determine which environment configuration to load.
#
ENV=staging